#Train, val, and test data in hkl format numpy array (numImages,numFrames, channels, pixels, pixels)
DATA_DIR = './iarpa_data/'

# Where model weights and config will be saved if you run kitti_train.py
# If you directly download the trained weights, change to appropriate path.
WEIGHTS_DIR = './iarpa_model/'

# Where results (prediction plots and evaluation file) will be saved.
RESULTS_SAVE_DIR = './iarpa_results/'
